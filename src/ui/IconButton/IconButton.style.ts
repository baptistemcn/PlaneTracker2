import { makeStyles } from "@rneui/themed";

export const useStyles = makeStyles(() => ({
  container: {
    width: 128,
    height: 48,
    borderRadius: 25,
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
}));
